/**
 * Contains any relay server code. The relay server acts as an intermediary between FA (the preferences) and FAF (the lobby).
 * Think of it like a locally running GPG server that redirects data from/to the FAF server.
 */
package com.faforever.client.fa.relay;
